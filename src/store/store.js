import { createStore } from 'vuex';

const store = createStore({
  state: {
    // Your state variables
  },
  mutations: {
    // Your mutations
  },
  actions: {
    // Your actions
  },
  modules: {
    // Optional: Vuex modules
  },
});

export default store;
